# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=librecov_dev
DB_SSLMODE=disable

# Server Configuration
PORT=4000

# OIDC Configuration (Optional)
# Uncomment and configure these to enable OIDC authentication
# OIDC_ISSUER=https://your-oidc-provider.com
# OIDC_CLIENT_ID=your-client-id
# OIDC_REDIRECT_URL=http://localhost:4000/auth/callback
# OIDC_SCOPES=openid,email,groups
# OIDC_GROUPS_CLAIM=groups

# Admin Configuration
# Set the email of the first admin user (will be set as admin on first login)
# FIRST_ADMIN_EMAIL=admin@example.com

# Session Secret (generate a random string in production)
SECRET_KEY_BASE=changeme_in_production
